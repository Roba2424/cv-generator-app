//regex
export const regexpValidation = /^(?=.*[A-Z])(?=.*\d.*\d.*\d)[A-Za-z\d]{5,15}$/;

//ROUTES
export const ROUTE_CONSTANTS = {
  LOGIN: "/login",
  REGISTER: "/register",
  PROFILE: "/profile",
};
